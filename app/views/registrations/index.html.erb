<header>
  <h1>
    Registrations
  </h1>
</header>

<ul id="registrations"> 
  <% @registrations.each do |r| %>
    <li>
      <%= mail_to(r.email, r.name) %>
      (<%= r.event.name %>)
    </li>
  <% end %>
</ul> 